namespace LibrarySystem.Infrastructure.Enums
{
    // This enum is used to specify the type of filter value being processed in the BooksFilterLinkTagHelper.
    public enum FilterValueFor
    {
        Top,
        Genre,
        Format,
        SearchTerm,
        Page
    }
}
